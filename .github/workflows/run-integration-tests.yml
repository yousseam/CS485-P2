name: Run Integration Tests

on:
  push:
    branches:
      - main
      - 'p6/**'
  pull_request:
    branches:
      - main
      - 'p6/**'

jobs:
  integration-tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20]

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_DB: test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install root devDependencies (cross-env)
        run: npm ci

      - name: Install backend dependencies
        working-directory: ./backend
        run: npm install

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Set up database environment
        working-directory: ./backend
        run: |
          echo "DB_HOST=localhost" >> .env.test
          echo "DB_PORT=5432" >> .env.test
          echo "DB_NAME=test_db" >> .env.test
          echo "DB_USER=test_user" >> .env.test
          echo "DB_PASSWORD=test_password" >> .env.test
          echo "NODE_ENV=test" >> .env.test

      - name: Run backend integration tests (database models)
        working-directory: ./backend
        run: node --test tests/integration.test.js
        env:
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: test_db
          DB_USER: test_user
          DB_PASSWORD: test_password
          NODE_ENV: test
        continue-on-error: true

      - name: Start backend server for API tests
        working-directory: ./backend
        run: |
          # Start backend in background with logging
          mkdir -p logs
          nohup npm run dev > logs/backend.log 2>&1 &
          echo $! > logs/backend.pid

          # Wait for backend to be ready with health checks
          for i in {1..30}; do
            echo "Waiting for backend to start... (attempt $i/30)"
            if curl -f http://localhost:3001/api/health > /dev/null 2>&1; then
              echo "Backend is ready!"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "Backend failed to start within 30 seconds"
              echo "Backend logs:"
              cat logs/backend.log
              exit 1
            fi
            sleep 1
          done
        env:
          PORT: 3001
          NODE_ENV: test
          DB_HOST: localhost
          DB_PORT: 5432
          DB_NAME: test_db
          DB_USER: test_user
          DB_PASSWORD: test_password

      - name: Run HTTP integration tests against local API
        env:
          INTEGRATION_API_BASE_URL: http://localhost:3001/api
          INTEGRATION_TARGET: local
        run: npm run test:integration
        continue-on-error: true

      - name: Run frontend tests
        working-directory: ./frontend
        run: npm test
        continue-on-error: true

      - name: Run frontend US2 tests
        working-directory: ./frontend
        run: npm run test:frontend-us2
        continue-on-error: true

      - name: Display backend logs if tests failed
        if: failure()
        working-directory: ./backend
        run: |
          echo "Backend logs:"
          cat logs/backend.log || echo "No backend logs found"

  integration-tests-cloud:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install root devDependencies (cross-env)
        run: npm ci

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      - name: Set environment for cloud tests
        run: |
          if [ -z "${{ vars.INTEGRATION_API_BASE_URL }}" ]; then
            echo "Warning: INTEGRATION_API_BASE_URL not set, skipping cloud tests"
            echo "Set this variable in GitHub repo Settings → Secrets and variables → Actions → Variables"
            exit 0
          fi

      - name: Run HTTP integration tests against deployed API
        env:
          INTEGRATION_API_BASE_URL: ${{ vars.INTEGRATION_API_BASE_URL }}
          INTEGRATION_TARGET: cloud
        run: npm run test:integration
